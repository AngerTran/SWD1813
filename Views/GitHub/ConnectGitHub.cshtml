@model SWD1813.Models.Project
@{
    ViewData["Title"] = "Connect GitHub";
    var defaultRepo = ViewBag.DefaultRepoUrl as string;
}

<h2>Connect GitHub - @Model.ProjectName</h2>
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}
@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}
<p class="text-muted small">Token: quyền đọc repo (<code>repo</code> nếu private). URL repo dùng để đồng bộ commit (VD: https://github.com/owner/repo). Repo <strong>public</strong> có thể chỉ cần URL, không bắt buộc PAT.</p>
<p class="alert alert-warning small py-2 mb-2"><strong>Quan trọng:</strong> Dùng ô bên dưới (dạng text) để <strong>dán PAT bằng Ctrl+V</strong>. Ô kiểu password thường bị trình duyệt chặn, server sẽ không nhận được token.</p>
<form asp-controller="GitHub" asp-action="ConnectGitHub" asp-route-id="@Model.ProjectId" method="post">
    <input type="hidden" name="projectId" value="@Model.ProjectId" />
    <div class="form-group mb-2">
        <label for="repoUrl">Repository URL</label>
        <input type="url" id="repoUrl" name="repoUrl" class="form-control" value="@defaultRepo" placeholder="https://github.com/owner/repo" />
    </div>
    <div class="form-group mb-2">
        <label for="githubToken">GitHub Personal Access Token (tùy chọn nếu repo public)</label>
        <input type="text" id="githubToken" name="githubToken" class="form-control font-monospace" autocomplete="off" spellcheck="false" placeholder="ghp_..." />
    </div>
    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-controller="Projects" asp-action="Details" asp-route-id="@Model.ProjectId" class="btn btn-secondary">Cancel</a>
</form>
<p class="text-muted small mt-3">Sau khi <strong>Save</strong>: chi tiết dự án → <strong>Đồng bộ GitHub</strong> → <strong>Xem commit &amp; đóng góp GitHub</strong> (danh sách commit + % theo tác giả).</p>
