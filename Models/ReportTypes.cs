namespace SWD1813.Models;

/// <summary>Giá trị ReportType dùng trong bảng reports và khi tải lại nội dung.</summary>
public static class ReportTypes
{
    public const string ProjectSummaryMarkdown = "Tóm tắt dự án (Markdown)";
    public const string SrsMarkdown = "SRS (Markdown)";
    public const string SrsPreview = "SRS (xem trước)";
}
